Place here empty directories named as your hosts, and launch command "vagrant provision --provision-with vhosts" or "vagrant reload". If your LAMP stack is ready to use, this will create virtual hosts on guest machine, and initialise your directories with some basic structure.
